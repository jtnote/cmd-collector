== backend: server start==
# make sure using powershell (test in 2.0 only)
# install python 3.7 above
cd server
python -m venv venv
pip install -r requirements.txt
.\startprod.ps1

== frontend: build ==
cd web
npm i
npm run build
# then open browser