bitbucket repo create tool(desktop) //web ver too?

onUpdateComplete

change indent size in VS code corresponding to Index.js

different title of edit and add

list page button using font awesome:
https://versions.bulma.io/0.7.1/documentation/elements/button/


bug: if url="www.pip.com", then link will be "localhost:5000/www.pip.com"

auto complete (e.g. keyword --- powershell)

* quick copy of command in list

comments?

[.] pagination

line break saved in db? should be saved in db as linebreak?

list button height too big?

web: if page<=3
web: if final result not full page, then paging bar position changes

==latency==
after user click "edit"
after user click "cancel" in edit page
after user click "cancel" in add page //reload is not necessary?